<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All chats</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB"
      crossorigin="anonymous"
    />

    <style>
      body {
        margin-left: 20px;
        background-color: black;
        color: beige;
      }
      .chat {
        background-color: cadetblue;
        display: inline-block;
        justify-content: center;

        padding: 10px;
        border-radius: 16px;
        color: burlywood;
      }
      .mgs {
        background-color: black;
        padding: 20px;
        border-radius: 10px;
      }
    </style>
  </head>
  <body>
    <h3>Here are all chats</h3>

    <form method="get" action="/chat/new">
      <button type="button" class="btn btn-primary">Create new chat</button>
    </form>
    <br />

    <%for (let chat of chats){%>
    <div class="chat">
      <p>From: <b> <%= chat.from %></b></p>
      <p>To: <b> <%= chat.to %></b></p>
      <div class="mgs">
        <p>Message: <i><%= chat.mgs %></i></p>
        <form method="get" action="/chat/<%= chat._id %>/edit">
          <button type="button" class="btn btn-warning">Edit</button>
        </form>
      </div>
      <p>
        Time: <%= chat.created_at.toString().split(" ").slice(0,5).join(" ") %>
      </p>
      <hr />
      <form method="post" action="/chat/<%= chat._id %>?_method=DELETE">
        <button type="button" class="btn btn-danger">Delete</button>
      </form>
    </div>
    <br />
    <br />

    <% }%>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
